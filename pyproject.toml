[project]
name = "peter"
version = "0.1.0"
description = "CLI tool for managing daily todos using prompt-toolkit"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "prompt-toolkit>=3.0.0",
]
[project.scripts]
peter = "peter.__main__:main"
